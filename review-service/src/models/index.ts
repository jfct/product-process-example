// models/index.ts
import './product.model'
import './review.model'

export function initializeModels() {
    // This function can be empty if you just need to ensure the files are executed
    // You can also add any additional model setup logic here if needed
}

